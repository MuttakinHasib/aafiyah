import AddressBookScreen from '../screens/address-book.screen';

export const metadata = {
  title: 'Address Book',
};

const AddressBookPage = async () => <AddressBookScreen />;

export default AddressBookPage;
